# Legacy MongoDB Manifests

Os arquivos deste diretório representam a estrutura legada do MongoDB.

Eles foram mantidos apenas como referência histórica. Para homolog, o caminho suportado é:

```bash
kubectl apply -k infra/kubernetes/mongodb/overlays/hml
```
