apiVersion: v1
kind: ConfigMap
metadata:
  name: urbana-connect-mongodb-config
data:
  mongod.conf: |
    storage:
      dbPath: /data/db
    net:
      bindIp: 0.0.0.0
      port: 27017
    security:
      authorization: enabled
    setParameter:
      enableLocalhostAuthBypass: false
