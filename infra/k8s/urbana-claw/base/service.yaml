apiVersion: v1
kind: Service
metadata:
  name: urbana-claw
  labels:
    app: urbana-claw
spec:
  type: ClusterIP
  selector:
    app: urbana-claw
  ports:
    - name: gateway
      port: 18789
      targetPort: gateway
      protocol: TCP
