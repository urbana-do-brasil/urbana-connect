apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: urbana-claw-home-pvc
  labels:
    app: urbana-claw
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
