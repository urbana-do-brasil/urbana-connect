# Legacy Manifests

Os arquivos deste diretório representam a estrutura monolítica antiga da app.

Eles foram mantidos apenas como referência histórica. Para homolog, o caminho suportado é:

```bash
kubectl apply -k infra/k8s/app/overlays/hml
```
