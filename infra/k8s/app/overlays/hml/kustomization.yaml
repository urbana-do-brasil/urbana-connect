apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: urbana-connect-hml

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: deployment-patch.yaml
  - path: configmap-patch.yaml

images:
  - name: registry.digitalocean.com/urbana-connect-registry/urbana-connect
    newTag: latest
