apiVersion: v1
kind: Service
metadata:
  name: urbana-connect-mongodb
  labels:
    app.kubernetes.io/name: urbana-connect-mongodb
spec:
  ports:
    - name: mongodb
      port: 27017
      targetPort: 27017
  selector:
    app.kubernetes.io/name: urbana-connect-mongodb
