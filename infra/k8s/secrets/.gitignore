# Ignora todos os arquivos no diretório prod
/prod/*
# Não ignora o arquivo .gitkeep
!/prod/.gitkeep

# Ignora os arquivos de secrets reais que possam estar nos templates
/templates/*-secret.yaml
/templates/mongodb-simple.yaml

# Não ignora os templates
!/templates/*-template.yaml

# Ignora o arquivo mongodb-simple.yaml original (com credenciais)
mongodb-simple.yaml

# Não ignora o template do mongodb-simple
!mongodb-simple-template.yaml 