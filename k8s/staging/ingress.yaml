apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-name
  namespace: staging
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - staging.seudominio.com.br
    secretName: staging-tls
  rules:
  - host: staging.seudominio.com.br
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-name
            port:
              number: 80 